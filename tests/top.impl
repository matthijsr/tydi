impl compositions.Top_level {
   /*Instantiation example*/
   inst1 : primitives.Magic[param := 5];
   /*Single connection example*/
   map_i : this.in.map(primitives.Sqrt);
   inst1.in <= map_i.out;
   this.out <= inst1.out;
}